Add Discount to Items

Your Current Items:
<% @items.each do |item| %>
  <%= item.name %>
  Original Price: <%= item.price %>
  Current Inventory Amount: <%= item.inventory %>
<% end %>

<%= form_tag "/merchant/discounts", method: :post do %>
Select Items to Apply Discount To:
<%= collection_check_boxes(:merchant, :item_id, @items, :id, :name) %>
<%= label_tag "Bulk Quantity Required For Discount (must be > 1)" %>
<%= text_field_tag :quantity %>
<%= label_tag "Discount Percentage (at least 0.01 to 0.99)" %>
<%= text_field_tag :percentage %>

<%= submit_tag "Submit" %>
<% end %>
